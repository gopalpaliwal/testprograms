ICMP Protocol Monkey - v0.1

To build the protocol monkey, follow the steps below:

tar xvf ICMP_PM_0.1_PROTOTYPE.tar
cd pm
make clean
make

All the binaries (pm executable in this case) will be located under pm/bin directory. To run the program:

su (become root)
cd bin
modprobe ip_queue
./pm

Use './pm -h' to see more usage options.

Sreevathsa
04/24/2007
